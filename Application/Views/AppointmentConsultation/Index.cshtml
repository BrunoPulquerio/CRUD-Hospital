@model IEnumerable<Service.ViewModels.AppointmentConsultationViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExamDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsultationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExamRegistrationId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PatientId)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExamDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsultationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExamRegistrationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PatientId)
            </td>
                        
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id })
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
